---
apiVersion: v1
kind: Service
metadata:
  name: squest
  namespace: squest
spec:
  type: NodePort
  selector:
    app: squest-web
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
